# Development Log: 2026-05-18

## Session Goal

Start phase 3 by adding a deep learning recommendation model.

## Planned Work

- Add PyTorch as a project dependency.
- Build a Neural Collaborative Filtering model with user and movie embeddings.
- Train with BPR ranking loss.
- Add early stopping based on validation BPR loss.
- Start with a small-sample smoke test before attempting full-data training.

## Progress

- Added `torch` to `requirements.txt`.
- Added `NEURAL_CF_MODEL_PATH` to `src/config.py`.
- Added `src/deep_learning/`.
- Added dataset utilities for positive interaction loading and negative sampling.
- Added `NeuralCollaborativeFiltering` with embeddings, ReLU MLP layers, dropout, and BPR loss.
- Added `src.deep_learning.train_neural_cf` training command with early stopping.
- Installed `torch==2.12.0` in the current Anaconda Python environment.
- Fixed BPR loss to use `torch.nn.functional.logsigmoid` for PyTorch compatibility.
- Normalized Neural CF checkpoint metadata to native Python types for safer `torch.load` behavior.
- Filtered positive interactions that cannot support negative sampling in small truncated datasets.

## Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- `python -c 'import torch; print(torch.__version__)'`: passed with `2.12.0`.
- Smoke test command:

```bash
python -m src.deep_learning.train_neural_cf --max-rows 20000 --epochs 2 --batch-size 512 --patience 2 --output /private/tmp/neural_cf_smoke.pt
```

Result:

- Train positives: `8548`
- Validation positives: `949`
- Epoch 1 validation BPR loss: `0.6768`
- Epoch 2 validation BPR loss: `0.5889`
- Best validation BPR loss: `0.5889`
- Checkpoint load verification: passed.

## Next TODO

- Add Neural CF recommendation/evaluation command for `HitRate@K`, `Precision@K`, and `NDCG@K`.
- Decide whether to run a larger/full Neural CF training job after evaluation code is available.

## Session 2 Goal

Complete the Neural CF evaluation loop with ranking metrics.

## Session 2 Progress

- Added `src.deep_learning.evaluate_neural_cf`.
- Added Neural CF checkpoint loading and batched candidate scoring.
- Added `HitRate@K`, `Precision@K`, and `NDCG@K` evaluation for Neural CF.
- Added optional CSV export for Neural CF metrics.
- Added extra positive-row scanning for partial smoke-test checkpoints.
- Updated `README.md` with Neural CF evaluation commands.

## Session 2 Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- The small `max_rows=20000` checkpoint had no eligible test rows because its user/movie mapping was too narrow.
- Trained a broader smoke checkpoint:

```bash
python -m src.deep_learning.train_neural_cf --max-rows 200000 --epochs 1 --batch-size 2048 --patience 1 --output /private/tmp/neural_cf_eval_smoke.pt
```

Result:

- Train positives: `107458`
- Validation positives: `11939`
- Best validation BPR loss: `0.3435`

Evaluation command:

```bash
python -m src.deep_learning.evaluate_neural_cf --model-path /private/tmp/neural_cf_eval_smoke.pt --top-k 100 --max-users 50 --scan-multiplier 100 --output /private/tmp/neural_cf_metrics_smoke.csv
```

Result:

- Evaluated items: `10`
- Skipped items: `2059`
- `HitRate@100`: `1.0000`
- `Precision@100`: `0.0100`
- `NDCG@100`: `0.3449`
- Avg candidate movies: `26.0`

Important note:

- These smoke metrics are not comparable with the full TruncatedSVD evaluation because the partial Neural CF checkpoint only knows a small subset of movies, so the candidate set is much smaller.

## Session 2 Next TODO

- Run full-data Neural CF training before making model-quality claims.
- Re-run Neural CF evaluation on the full checkpoint so the candidate space is comparable to the SVD evaluation.

## Session 3 Progress

- Started a full-data Neural CF training run, but it stayed in data preparation too long to be a practical default workflow.
- Added `--max-positive-samples` to sample positive interactions after loading the full processed ratings file.
- Updated README with a practical sampled Neural CF training command.

## Session 4 Full-Training Fix

- The earlier full-data Neural CF run eventually completed and saved `models/neural_cf_model.pt`.
- Confirmed full checkpoint metadata:
  - users: `472540`
  - movies: `5000`
  - embedding dim: `32`
  - hidden dims: `[64, 32]`
  - best validation BPR loss: `0.1017`
- Root issue: full training was not actually frozen; the data-preparation phase had no progress output and was too opaque.
- Refactored `load_interaction_data()` to read `train_rating.csv` in chunks.
- Added progress output for:
  - pass 1: user/movie mapping scan
  - pass 2: rated movie set and positive interaction construction
- Added `--chunk-size` and `--quiet-data-loading` to `src.deep_learning.train_neural_cf`.

## Session 4 Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- Full Neural CF checkpoint evaluation:

```bash
python -m src.deep_learning.evaluate_neural_cf --model-path models/neural_cf_model.pt --top-k 100 --max-users 100 --output /private/tmp/neural_cf_full_metrics_k100.csv
```

Result:

- `HitRate@100`: `0.0900`
- `Precision@100`: `0.0009`
- `NDCG@100`: `0.0271`
- Avg candidate movies: `4682.6`

SVD comparison:

```bash
python -m src.ranking_metrics --top-k 100 --max-users 100 --output /private/tmp/svd_metrics_k100_compare.csv
```

Result:

- `HitRate@100`: `0.0800`
- `Precision@100`: `0.0008`
- Avg candidate movies: `4682.6`

Chunked loader smoke test:

```bash
python -m src.deep_learning.train_neural_cf --max-rows 20000 --epochs 1 --batch-size 2048 --patience 1 --chunk-size 5000 --output /private/tmp/neural_cf_chunked_loader_test.pt
```

Result:

- Progress output appeared for both data-loading passes.
- Training completed and saved checkpoint successfully.
