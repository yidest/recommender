# Development Log: 2026-05-19

## Session Goal

Fix the full-data Neural CF training workflow so it is observable and practical.

## Completed

- Confirmed the previous full-data Neural CF training run eventually completed.
- Confirmed `models/neural_cf_model.pt` is a full checkpoint with:
  - `472540` users
  - `5000` movies
  - best validation BPR loss `0.1017`
- Identified the real problem: the data preparation phase had no progress output, so full training looked frozen.
- Refactored Neural CF data loading to use chunked two-pass CSV scanning.
- Added chunk-level progress output during:
  - user/movie mapping construction
  - rated-item set and positive interaction construction
- Added `--chunk-size` and `--quiet-data-loading` training options.
- Evaluated the full Neural CF checkpoint and compared it against TruncatedSVD.

## Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- Chunked loader smoke test passed with visible progress output.
- Full Neural CF evaluation:
  - `HitRate@100`: `0.0900`
  - `Precision@100`: `0.0009`
  - `NDCG@100`: `0.0271`
- TruncatedSVD comparison:
  - `HitRate@100`: `0.0800`
  - `Precision@100`: `0.0008`

## Decision

Keep the full Neural CF checkpoint for now because it slightly improves `HitRate@100` over TruncatedSVD on the current sample evaluation.

## Next TODO

- Add a Neural CF recommendation command for individual users.
- Add a shared comparison report command for SVD vs Neural CF.
- Consider optimizing checkpoint size and metadata storage before GitHub upload.

## Session 2 Progress

- Added `src.deep_learning.recommend_neural_cf` for individual-user Neural CF recommendations.
- Added `src.compare_models` for side-by-side TruncatedSVD vs Neural CF ranking comparison.
- Added `NDCG@K` to the TruncatedSVD ranking metrics so both model families report the same ranking metric set.
- Updated `README.md` with Neural CF recommendation and comparison commands.

## Session 2 Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- Neural CF recommendation command passed:

```bash
python -m src.deep_learning.recommend_neural_cf --user-id 1488844 --top-n 5 --output /private/tmp/neural_cf_recs_user_1488844.csv
```

Top result:

- `Clerks`, score `2.6288`

- TruncatedSVD ranking metrics with `NDCG@100` passed:
  - `HitRate@100`: `0.0800`
  - `Precision@100`: `0.0008`
  - `NDCG@100`: `0.0230`
- Model comparison command passed:

```bash
python -m src.compare_models --top-k 100 --max-users 100 --output /private/tmp/model_comparison_k100.csv
```

Comparison:

| model | HitRate@100 | Precision@100 | NDCG@100 |
| --- | --- | --- | --- |
| TruncatedSVDModel | 0.0800 | 0.0008 | 0.0230 |
| NeuralCollaborativeFiltering | 0.0900 | 0.0009 | 0.0271 |

## Session 3 Goal

Start Neural CF data-processing optimization before further full-data model tuning.

## Session 3 Plan

- Add a reusable prepared-data cache for Neural CF interactions.
- Add a standalone prepare command so slow full CSV scanning can be run once.
- Update Neural CF training to reuse the prepared cache by default.

## Session 3 Completed

- Added `NEURAL_CF_DATA_CACHE_PATH`.
- Added Neural CF interaction cache read/write helpers.
- Added `src.deep_learning.prepare_neural_cf_data`.
- Updated `src.deep_learning.train_neural_cf` to load matching caches by default.
- Protected the default full-data cache from `--max-rows` smoke runs.
- Added training flags:
  - `--data-cache-path`
  - `--rebuild-data-cache`
  - `--no-data-cache`
- Kept `--max-positive-samples` as a training-time sample applied after loading the full prepared data.
- Updated `README.md` with the prepare-cache workflow.

## Session 3 Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- `python -m src.deep_learning.prepare_neural_cf_data --max-rows 20000 --chunk-size 5000 --output /private/tmp/neural_cf_cache_smoke.pkl --force`: passed.
- `python -m src.deep_learning.train_neural_cf --max-rows 20000 --epochs 1 --batch-size 2048 --patience 1 --data-cache-path /private/tmp/neural_cf_cache_smoke.pkl --output /private/tmp/neural_cf_cache_train_smoke.pt`: passed and loaded the cache instead of rescanning CSV.
- `python -m src.deep_learning.train_neural_cf --max-rows 20000 --epochs 1 --batch-size 2048 --patience 1 --quiet-data-loading --output /private/tmp/neural_cf_default_cache_guard_smoke.pt`: passed without using the default full-data cache.
- `python -m src.deep_learning.prepare_neural_cf_data --max-rows 20000`: failed intentionally with a clear error requiring a custom `--output` for partial caches.

## Session 3 Next TODO

- Build the default full Neural CF cache with `python -m src.deep_learning.prepare_neural_cf_data`.
- Measure full-cache load time versus full CSV preparation time.
- Consider a more compact cache format if the pickle becomes too large.

## Session 4 Goal

Continue Neural CF data-processing optimization by reducing CSV scans during cache preparation.

## Session 4 Completed

- Refactored `load_interaction_data()` from two-pass CSV scanning to one-pass dynamic mapping.
- The loader now builds user/movie mappings, rated-item sets, and positive pairs in a single chunk loop.
- Kept the same positive filtering behavior for users with no available negative samples.

## Session 4 Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- `python -m src.deep_learning.prepare_neural_cf_data --max-rows 20000 --chunk-size 5000 --output /private/tmp/neural_cf_onepass_cache_smoke.pkl --force`: passed.
  - Output showed one chunk pass instead of separate pass 1/pass 2 progress.
  - Final counts matched the previous smoke cache: `19,272` users, `8` movies, `9,497` usable positive pairs.
- `python -m src.deep_learning.train_neural_cf --max-rows 20000 --epochs 1 --batch-size 2048 --patience 1 --data-cache-path /private/tmp/neural_cf_onepass_cache_smoke.pkl --output /private/tmp/neural_cf_onepass_train_smoke.pt`: passed and loaded the cache.
- `time python -m src.deep_learning.prepare_neural_cf_data --max-rows 200000 --chunk-size 50000 --output /private/tmp/neural_cf_onepass_cache_200k.pkl --force`: passed in about `1.407s` wall time.

## Session 4 Next TODO

- Build and time the default full-data cache.
- Measure training startup time with the full cache.
- If the full pickle cache is too large or slow to load, move to a more compact tensor/array-backed cache format.

## Session 5 Goal

Continue Neural CF optimization roadmap:

- Fixed benchmark with Popularity baseline.
- Configurable negative sampling.
- Full-cache training smoke run.

## Session 5 Completed

- Added `src.benchmark_models`.
- The benchmark compares:
  - `PopularityBaseline`
  - `TruncatedSVDModel`
  - `NeuralCollaborativeFiltering`
- The benchmark uses the same eligible held-out positive rows for all model families.
- Added multi-K benchmark support, for example `--top-k 10 50 100`.
- Added BPR negative sampling strategies:
  - `random`
  - `popularity`
  - `mixed`
- Optimized popularity-based negative sampling with precomputed cumulative weights.
- Added training options:
  - `--negative-sampling-strategy`
  - `--negative-popularity-power`
  - `--mixed-negative-probability`
- Built the default full Neural CF interaction cache at `data/processed/neural_cf_interactions.pkl`.

## Session 5 Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- `python -m src.deep_learning.train_neural_cf --max-rows 20000 --epochs 1 --batch-size 2048 --patience 1 --negative-sampling-strategy mixed --mixed-negative-probability 0.5 --quiet-data-loading --output /private/tmp/neural_cf_mixed_negative_smoke.pt`: passed.
- `python -m src.deep_learning.train_neural_cf --max-rows 20000 --epochs 1 --batch-size 2048 --patience 1 --negative-sampling-strategy popularity --quiet-data-loading --output /private/tmp/neural_cf_popularity_negative_smoke.pt`: passed.
- `python -m src.benchmark_models --top-k 10 50 --max-users 5 --output /private/tmp/benchmark_smoke.csv`: passed.
- `python -m src.benchmark_models --top-k 10 50 100 --max-users 100 --output /private/tmp/benchmark_k10_50_100_users100.csv`: passed.

Benchmark sample result:

| model | K | HitRate | Precision | NDCG |
| --- | ---: | ---: | ---: | ---: |
| PopularityBaseline | 10 | 0.0100 | 0.0010 | 0.0029 |
| TruncatedSVDModel | 10 | 0.0100 | 0.0010 | 0.0100 |
| NeuralCollaborativeFiltering | 10 | 0.0200 | 0.0020 | 0.0133 |
| PopularityBaseline | 50 | 0.0400 | 0.0008 | 0.0095 |
| TruncatedSVDModel | 50 | 0.0400 | 0.0008 | 0.0165 |
| NeuralCollaborativeFiltering | 50 | 0.0600 | 0.0012 | 0.0220 |
| PopularityBaseline | 100 | 0.0600 | 0.0006 | 0.0128 |
| TruncatedSVDModel | 100 | 0.0800 | 0.0008 | 0.0230 |
| NeuralCollaborativeFiltering | 100 | 0.0900 | 0.0009 | 0.0271 |

Full cache preparation:

- `time python -m src.deep_learning.prepare_neural_cf_data --force`: passed.
- Final cache counts:
  - users: `472,540`
  - movies: `5,000`
  - positive pairs: `15,178,327`
- Wall time: about `51.168s`.

Full-cache training smoke:

- `time python -m src.deep_learning.train_neural_cf --max-positive-samples 200000 --epochs 1 --batch-size 4096 --patience 1 --negative-sampling-strategy mixed --mixed-negative-probability 0.5 --output /private/tmp/neural_cf_fullcache_mixed_200k_smoke.pt`: passed.
- Loaded the full interaction cache instead of scanning CSV.
- Train positives: `180,000`
- Validation positives: `20,000`
- Validation BPR loss after one epoch: `0.6038`
- Wall time: about `54.200s`.
- Smoke checkpoint benchmark at `K=100` passed:
  - `NeuralCollaborativeFiltering`: HitRate `0.0700`, NDCG `0.0141`
  - This is only a smoke checkpoint, not a quality candidate.

## Session 5 Next TODO

- Run full-cache Neural CF training with `mixed` negative sampling for more epochs.
- Evaluate the resulting checkpoint with `src.benchmark_models --top-k 10 50 100 --max-users 1000`.
- Start small hyperparameter grid after the mixed-sampling full run:
  - embedding dim: `32`, `64`
  - dropout: `0.1`, `0.2`
  - learning rate: `0.001`, `0.0005`

## Session 6 Goal

Update README project positioning after benchmark results showed Neural CF is the strongest current ranking model.

## Session 6 Completed

- Rewrote `README.md` around Neural CF as the main recommendation model.
- Repositioned TruncatedSVD, Bias, GlobalMean, and Popularity as baselines.
- Updated the project structure to include:
  - `src/benchmark_models.py`
  - `src/compare_models.py`
  - `src/deep_learning/prepare_neural_cf_data.py`
  - `docs/`
  - `dev_logs/`
  - `reports/`
- Replaced stale Neural CF wording that described it as a separate add-on model.
- Updated the main workflow order:
  - prepare ratings
  - prepare Neural CF cache
  - train Neural CF
  - recommend with Neural CF
  - benchmark against baselines
- Kept the legacy TruncatedSVD recommendation command documented as a baseline workflow.

## Session 6 Decision

- Neural CF is now the project's main model because it is the best current model on fixed ranking benchmarks.
- TruncatedSVD remains useful as a traditional collaborative filtering baseline and should not be removed.

## Session 6 Verification

- Read through the rewritten `README.md`.
- Checked current source files against the documented project structure.
- Confirmed `reports/` is documented as a generated output directory; it may not exist until commands write reports.

## Session 6 Follow-up

- Removed the Neural CF ranking-score note from `Current Limitations`.
- Kept the note near recommendation output because it describes score semantics, not an unresolved project limitation.

## Session 7 Goal

Make the code-level recommendation entry point match the README positioning: Neural CF is the main recommender.

## Session 7 Completed

- Updated `src.recommend` so the default recommendation model is Neural CF.
- Added `--model neural-cf|svd` to the unified recommendation command.
- Kept TruncatedSVD recommendations available with `--model svd`.
- Added Neural CF options to `src.recommend`:
  - `--model-path`
  - `--batch-size`
  - `--device`
- Kept SVD options:
  - `--svd-model-path`
  - `--retrain`
- Updated `README.md` so `python -m src.recommend` is the main Neural CF recommendation command.

## Session 7 Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- `python -m src.recommend --user-id 1488844 --top-n 3`: passed and returned Neural CF recommendations.
- `python -m src.recommend --model svd --user-id 1488844 --top-n 3`: passed and returned TruncatedSVD recommendations.

## Session 7 Follow-up

- Audited the rest of `README.md` after making `src.recommend` default to Neural CF.
- Added a main workflow block that starts with data processing, then Neural CF cache preparation, model training, recommendation, and benchmarking.
- Clarified that `train.py` is currently the preprocessing entry point and also trains traditional baselines.
- Renamed the baseline section to `Traditional Baselines`.
- Moved `src.compare_models` under baseline/older comparison tooling and documented `src.benchmark_models` as the preferred report command.
- Removed non-limitation notes from `Current Limitations`.
- Replaced remaining `legacy TruncatedSVD` wording with `TruncatedSVD baseline`.

## Session 8 Goal

Make final GitHub-readiness adjustments before publishing.

## Session 8 Completed

- Added README project status text:
  - learning and portfolio project
  - not a production recommendation service
  - raw data, processed data, trained checkpoints, caches, and generated reports are not included
- Added README artifact policy around ignored generated paths.
- Added README verification commands.
- Added a PyTorch install note because setup can vary by OS and hardware.
- Expanded `.gitignore` for common local-only files:
  - `.env`
  - `.mypy_cache/`
  - `.ruff_cache/`
  - `env/`

## Session 8 Verification

- `python -m py_compile train.py src/*.py src/deep_learning/*.py`: passed.
- `python -m src.recommend --help`: passed and shows `--model {neural-cf,svd}` with Neural CF as the default main model.
- Checked README for artifact policy and generated-path wording.
